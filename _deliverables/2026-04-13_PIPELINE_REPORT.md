# jenkins-s3-test Pipeline Report
**Date:** 2026-04-13
**Build:** #4 — SUCCESS
**Job:** `jenkins-s3-test`
**Jenkins:** https://jenkins.keepuneat.click/job/jenkins-s3-test/
**Repo:** NRD808Sequence/jenkins-s3-test → Jenkinsfile
**Branch:** main

---

## Build Result

| Field | Value |
|---|---|
| Build # | 4 |
| Result | SUCCESS |
| Stages | 7 / 7 |
| S3 Artifact Test | PASSED |
| TF Bucket Created | jenkins-bucket-20260413202615833100000001 |
| TF Bucket Destroyed | Yes — `terraform destroy` in same run |
| Pike Scan | PASS — `pike-policy.json` archived (2 statement blocks) |

---

## Pipeline Stages — Build #4

| # | Stage | Result | Notes |
|---|---|---|---|
| 1 | Set AWS Credentials | PASS | `aws sts get-caller-identity` → JenkinsTest01 |
| 2 | S3 Artifact Test | PASS | Upload → download → diff → delete against `class7-armagaggeon-tf-bucket` |
| 3 | TF Init | PASS | Backend: `class7-armagaggeon-tf-bucket/jenkins-s3-test/terraform.tfstate` |
| 4 | Pike Scan | PASS | `pike-policy.json` archived — 2 statement blocks (DynamoDB + S3) |
| 5 | TF Plan | PASS | 1 resource to add (`aws_s3_bucket.frontend`) |
| 6 | TF Apply | PASS | Bucket created — approved by admin |
| 7 | TF Destroy | PASS | Bucket destroyed — approved by admin |

---

## Pike Scan Results — Build #4

Minimum IAM policy generated by `jameswoolfenden/pike` scanning `1-auth.tf` + `2-main.tf`.
Archived as `pike-policy.json` build artifact.

```json
{
  "Statement": [
    {
      "Sid": "VisualEditor0",
      "Action": [
        "dynamodb:DeleteItem", "dynamodb:DescribeTable",
        "dynamodb:GetItem", "dynamodb:PutItem"
      ]
    },
    {
      "Sid": "VisualEditor1",
      "Action": [
        "s3:CreateBucket", "s3:DeleteBucket", "s3:DeleteObject",
        "s3:GetAccelerateConfiguration", "s3:GetBucketAcl", "s3:GetBucketCORS",
        "s3:GetBucketLogging", "s3:GetBucketObjectLockConfiguration",
        "s3:GetBucketPolicy", "s3:GetBucketRequestPayment", "s3:GetBucketTagging",
        "s3:GetBucketVersioning", "s3:GetBucketWebsite", "s3:GetEncryptionConfiguration",
        "s3:GetLifecycleConfiguration", "s3:GetObject", "s3:GetObjectAcl",
        "s3:GetReplicationConfiguration", "s3:ListBucket",
        "s3:PutBucketTagging", "s3:PutObject"
      ]
    }
  ]
}
```

**Note on DynamoDB:** Pike maps these from the S3 backend TF source. No DynamoDB lock table is configured (`1-auth.tf` has no `dynamodb_table =`). These 4 actions are not required by this pipeline.

Full output: `jenkins-s3-test/_deliverables/pike-policy.json`

---

## S3 Artifact Test

Round-trip test against `class7-armagaggeon-tf-bucket`:

```
Upload:   test-artifact.txt → s3://.../jenkins-artifacts/test-artifact-4.txt
Download: s3://.../jenkins-artifacts/test-artifact-4.txt → downloaded-artifact.txt
Verify:   diff test-artifact.txt downloaded-artifact.txt → PASSED
Cleanup:  aws s3 rm s3://.../jenkins-artifacts/test-artifact-4.txt
```

Proves `JenkinsTest01` has `s3:PutObject` + `s3:GetObject` on the TF state bucket.

---

## Terraform

| Field | Value |
|---|---|
| TF State Bucket | class7-armagaggeon-tf-bucket |
| TF State Key | jenkins-s3-test/terraform.tfstate |
| Resource Created | `aws_s3_bucket.frontend` → `jenkins-bucket-20260413202615833100000001` |
| Resource Destroyed | Same run — `terraform destroy -auto-approve` |
| IAM User | JenkinsTest01 (AKIATDDDPJRGCJXLPGTM) |

---

## Infrastructure Reference

| Resource | Value |
|---|---|
| Jenkins | https://jenkins.keepuneat.click |
| TF State | class7-armagaggeon-tf-bucket/jenkins-s3-test/terraform.tfstate |
| Credential ID | JenkinsTest01 |
